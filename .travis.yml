language: c
compiler:
    - gcc
    - clang
before_install:
    - sudo apt-get update -qq
    - sudo apt-get install -qq lua5.2 liblua5.2-dev libssl-dev libzmq3-dev libevent-dev
script: "scons && source ./test/test_all.sh"
